## 🧠 SSH免密码登录笔记（Windows → Linux）

### 一、目标

通过 **SSH公钥认证** 实现从 Windows（PowerShell / VSCode）登录 Linux 主机时 **无需输入密码**。

---

### 二、原理

SSH免密登录基于 **非对称加密**：

- **本地生成一对密钥**：公钥（公开）+ 私钥（保密）。
    
- **公钥存放到远程主机**，服务器根据公钥验证你身份。
    
- 下次连接时，系统会自动用私钥进行认证，无需密码。
    

---

### 三、操作步骤

#### 1. 在 Windows 上生成密钥

`ssh-keygen -t ed25519 -C "你的邮箱或备注"`

> 生成后文件路径：  
> 公钥 → `C:\Users\<用户名>\.ssh\id_ed25519.pub`  
> 私钥 → `C:\Users\<用户名>\.ssh\id_ed25519`

---

#### 2. 把公钥复制到远程主机

在 **PowerShell** 中执行：

`type $env:USERPROFILE\.ssh\id_ed25519.pub | ssh 用户名@主机IP "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"`

📘 示例：

`type $env:USERPROFILE\.ssh\id_ed25519.pub | ssh linmiaoju@192.168.1.100 "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"`

🧩 命令分解：

- `type $env:USERPROFILE\.ssh\id_ed25519.pub` → 读取本地公钥内容
    
- `ssh 用户名@主机IP` → 连接到远程服务器
    
- `mkdir -p ~/.ssh` → 创建 `.ssh` 文件夹（如果没有）
    
- `cat >> ~/.ssh/authorized_keys` → 把公钥追加到授权列表