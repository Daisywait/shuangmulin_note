> 适用于内网部署的 GitLab 实例（IP: `172.31.179.162`，端口: `8929`）

---

## 🔐 登录信息
- **访问地址**：http://172.31.179.162:8929/
- **用户名**：`linmiaoju`
- **密码**：初始密码（见下文）或您修改后的密码

---

## 🔑 查看初始 root 密码（仅首次有效）
> ⚠️ 必须在 **GitLab 容器内部** 执行！

```bash
# 进入容器
docker exec -it gitlab bash

# 查看初始密码
cat /etc/gitlab/initial_root_password
```

> 💡 该文件在首次启动后生成，若已删除则无法恢复。

---

## ⚙️ 核心配置文件
```bash
/etc/gitlab/gitlab.rb    # 主配置文件
```
修改后需重载：
```bash
gitlab-ctl reconfigure
```

---

## 🐳 容器管理命令

| 操作 | 命令 |
|------|------|
| 查看所有容器 | `docker ps -a` |
| 启动 GitLab | `docker start gitlab` |
| 停止 GitLab | `docker stop gitlab` |
| 删除容器 | `docker rm gitlab` |
| 进入容器调试 | `docker exec -it gitlab bash` |
| 查看日志 | `docker logs -f gitlab` |

---

## 🛠️ 高级调试工具

```bash
# 检查共享内存（GitLab 要求 ≥256MB）
docker exec gitlab df -h /dev/shm

# 进入 Rails 控制台（管理用户、项目等）
docker exec -it gitlab gitlab-rails console
```

---

## 🔌 SSH 连接配置（推荐方式）

### 1. 生成密钥（本地执行）
```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
```

### 2. 复制公钥
```bash
cat ~/.ssh/id_ed25519.pub
```

### 3. 添加到 GitLab
- 登录 → **Settings → SSH Keys** → 粘贴公钥

### 4. 测试连接（注意端口！）
```bash
ssh -T git@172.31.179.162 -p 8929
```
> ✅ 成功提示：`Welcome to GitLab, @linmiaoju!`

> 📌 注意：
> - SSH Key 是 **按用户配置**，不是按仓库
> - SSH 地址格式：`git@172.31.179.162:root/jepa.git`

---

## 🧪 克隆示例
```bash
# HTTPS 方式
git clone http://172.31.179.162:8929/root/jepa-ai-research.git

# SSH 方式（需配置好密钥）
git clone git@172.31.179.162:root/jepa-ai-research.git
```

---

✅ **适用场景**：部署维护、密码找回、SSH 配置、容器排错。

---

