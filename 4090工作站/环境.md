非常重要的工作站使用规范！我来帮你整理和补充：

## 📍 **存储使用规范**

### **系统盘 (/) - 916GB SSD**
- ✅ **可以放**：代码、配置文件、文档
- ❌ **不要放**：数据集、模型文件、大型缓存

### **数据盘 (/data) - 3.6TB HDD/SSD**
- ✅ **必须放**：数据集、训练结果、模型文件、大型缓存

## 🔧 **环境配置建议**

### **1. 设置 Huggingface 缓存到数据盘**
```bash
# 临时设置（当前会话有效）
export HF_HOME=/data/linmiaoju/.cache/huggingface

# 永久设置 - 添加到 ~/.bashrc
echo 'export HF_HOME=/data/linmiaoju/.cache/huggingface' >> ~/.bashrc
source ~/.bashrc
```

### **2. 设置 Conda 环境到数据盘**
```bash
# 设置conda环境目录到数据盘
echo 'export CONDA_ENVS_PATH=/data/linmiaoju/.conda/envs' >> ~/.bashrc
echo 'export CONDA_PKGS_DIRS=/data/linmiaoju/.conda/pkgs' >> ~/.bashrc
source ~/.bashrc
```

### **3. 设置其他缓存目录**
```bash
# 添加到 ~/.bashrc
echo 'export TORCH_HOME=/data/linmiaoju/.cache/torch' >> ~/.bashrc
echo 'export XDG_CACHE_HOME=/data/linmiaoju/.cache' >> ~/.bashrc
source ~/.bashrc
```

## 📁 **创建目录结构**
```bash
# 在数据盘创建规范的目录结构
mkdir -p /data/linmiaoju/{datasets,models,projects,cache,.conda}
mkdir -p /data/linmiaoju/cache/{huggingface,torch,other}

# 创建软链接（可选）
ln -s /data/linmiaoju/datasets ~/datasets
ln -s /data/linmiaoju/models ~/models
```

## 🌐 **网络访问说明**

### **校园网限制**
- 工作站需要**校园网环境**才能正常访问
- 如果在家需要先连接学校VPN
- 确认网络连接正常后再进行开发工作

## 💡 **最佳实践检查清单**

### **开始新项目时：**
```bash
# 1. 项目代码放在home
cd ~/projects/my_project

# 2. 数据放在数据盘
cp dataset.zip /data/linmiaoju/datasets/

# 3. 训练输出到数据盘
python train.py --output_dir /data/linmiaoju/models/my_model
```

### **定期检查磁盘使用：**
```bash
df -h /home/linmiaoju    # 检查home目录使用情况
df -h /data/linmiaoju    # 检查数据目录使用情况
du -sh ~/*               # 查看home下各目录大小
```

记得按照这个规范使用，避免系统盘爆满影响工作站正常运行！🚀