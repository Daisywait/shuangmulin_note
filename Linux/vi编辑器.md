
|操作|命令|说明|
|---|---|---|
|打开文件|`vi filename` 或 `vim filename`|新建或编辑文件|
|只读模式|`vi -R filename` 或 `view filename`|不能修改|
|退出（未保存）|`:q`|正常退出|
|强制退出|`:q!`|丢弃修改强制退出|
|保存并退出|`:wq` 或 `ZZ`（大写）|保存后退出|
|保存不退出|`:w`|仅保存|
|另存为|`:w newfile`|保存到新文件|
