#### 一、`plot()` —— 绘制函数或数据曲线

**语法：**

```matlab
plot(x, y)
```

**功能：**  
绘制连续变量 `x` 与 `y` 的关系，常用于展示**趋势**或**变化规律**。

**示例：**

```matlab
x = 0:0.1:2*pi;
y1 = sin(x);
y2 = cos(x);
plot(x, y1, 'r--', x, y2, 'b-')
title('Sine and Cosine')
xlabel('x')
ylabel('y')
legend('sin(x)', 'cos(x)')
grid on
axis([0 2*pi -1 1])
```

**特点：**

- 展示连续数据的变化趋势；
    
- 可叠加多条曲线；
    
- 可添加标题、坐标轴、图例、网格、坐标范围；
    
- 可通过 `subplot()` 创建多图布局。
    

---

#### 二、`histogram()` —— 绘制数据分布直方图

**语法：**

```matlab
histogram(data)
```

**功能：**  
统计一组数据在不同区间（bin）中的数量或频率，用矩形柱展示数据的**分布情况**。

**示例：**

```matlab
data = randn(1, 1000);
histogram(data, FaceColor='y', NumBins=20)
title('Data Distribution')
xlabel('Value')
ylabel('Frequency')
```

**常用参数：**

- `FaceColor`：设置柱形颜色
    
- `NumBins`：设置区间数量
    
- `Normalization='probability'`：显示概率分布
    

**特点：**

- 展示样本的分布密度；
    
- 常用于统计分析和数据探索；
    
- 横轴为数值区间，纵轴为频数或概率。
    

---

#### 三、`bar()` —— 绘制条形图（柱状图）

**语法：**

```matlab
bar(x, y)
```

**功能：**  
用于绘制**分类数据**或**离散型数据**的比较。

**示例：**

```matlab
x = categorical({'A','B','C','D'});
y = [5 8 3 6];
bar(x, y, FaceColor='c')
title('Category Comparison')
xlabel('Category')
ylabel('Value')
```

**特点：**

- 每个柱子对应一个分类；
    
- 通常用于对比不同类别的大小；
    
- 与 `histogram` 不同：`bar` 是你**自己提供每个类别的值**，而 `histogram` 是**自动统计区间频数**。
    

---

#### 四、三者对比总结

|函数|主要用途|横轴|纵轴|适用数据|图像类型|
|:-:|:--|:--|:--|:--|:--|
|`plot()`|连续变量的关系|自变量 `x`|函数值 `y`|连续数据|曲线图|
|`histogram()`|数据分布|数值区间 (bins)|频数或概率|样本数据|直方图|
|`bar()`|分类比较|分类名称|类别对应的数值|离散数据|条形图|

---

#### 🪄口诀

> `plot` 看趋势，  
> `histogram` 看分布，  
> `bar` 看分类比较。

---